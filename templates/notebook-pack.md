---
schema: notebook-source.v1
notebook: "{{ logical_notebook_id }}"
source_path: "{{ vault_relative_source_path }}"
source_sha256: "{{ lowercase_sha256 }}"
source_kind: "{{ source_kind }}"
classification: "{{ public_safe_or_approved_external }}"
repository: "{{ source_repository }}"
revision: "{{ source_commit }}"
---

# {{ Source title }}

> [!info] Exported source
> Generated by mq-agent from an explicitly allowed mqobsidian source. Verify
> claims against `source_path` and `revision`. This file is not canonical memory.

{{ reviewed_source_content }}
